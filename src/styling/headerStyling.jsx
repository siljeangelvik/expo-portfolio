import {StyleSheet} from 'react-native';

const HeaderStyling = StyleSheet.create({
    title: {
        fontSize: 36, // Adjust the font size as needed for a heading
        fontWeight: 'bold', // Make the heading bold
        marginBottom: 10, // Add spacing below the heading
    },
    heading: {
        fontSize: 24, // Adjust the font size as needed for a heading
        fontWeight: 'bold', // Make the heading bold
        marginBottom: 10, // Add spacing below the heading
    },
    paragraph: {
        fontSize: 16, // Adjust the font size as needed for a paragraph
        lineHeight: 24, // Set the line height for better readability
        color: '#333', // Set the color of the paragraph text
    },
});

export default HeaderStyling;